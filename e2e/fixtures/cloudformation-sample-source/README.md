# cloudformation-sample-source

Minimal CloudFormation tree consumed by `e2e/audit_test.go` to exercise
`cbx audit --source` against a CFN flavor. Contains intentionally
misconfigured resources; **do not** copy into real infrastructure.
