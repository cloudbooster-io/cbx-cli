# kubernetes-sample-source

Minimal Kubernetes tree consumed by `e2e/audit_test.go` to exercise
`cbx audit --source` against the K8s flavor. Contains intentionally
misconfigured workloads; **do not** copy into real clusters.
